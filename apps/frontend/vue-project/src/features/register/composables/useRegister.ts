
import { ref } from 'vue';
import type { IPet } from '@/types/pet'


export default function useRegister(form: IPet) {
    const loading = ref(false);
    const error = ref<string | null>(null);

    if(form.)
}